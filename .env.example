# Пример файла окружения для VodokanalStat
# Скопируйте этот файл в .env и заполните значения
#
# cp .env.example .env
#
# Замечания:
# - TG_API_ID и TG_API_HASH получите на https://my.telegram.org
# - TG_CHANNEL — исходный канал для анализа (username канала или его ID)
# - TG_SEND_TO — куда отправлять отчёт; по умолчанию берётся с TG_CHANNEL
# - TG_BOT_TOKEN — токен Telegram-бота, который будет отправлять сообщения (если не указан, отправка через клиент)
# - SEND_SILENT — «тихая» отправка: 1/true/yes/on включает, что угодно другое — выключает
# - TZ — часовой пояс контейнера

# Обязательные
TG_API_ID=12345678
TG_API_HASH=0123456789abcdef0123456789abcdef
TG_BOT_TOKEN=
TG_CHANNEL=@taganrog_vodokanal

# Необязательные
# Если оставить пустым, будет использоваться TG_CHANNEL
TG_SEND_TO=
# 1 | true | yes | on  -> включить «тихую» отправку сообщений
# что угодно другое    -> обычная отправка
SEND_SILENT=

# Часовой пояс контейнера (если не задан, по умолчанию Europe/Moscow)
TZ=Europe/Moscow
